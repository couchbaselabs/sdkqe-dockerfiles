Build Sync Gateway Docker image <br>
`./build.sh [version] [build]`<br>
Following example builds sync gateway 1.4.5-366<br>
`./build.sh 1.4.5 366` <br>
If no version is given, default is 1.4.5<br>
If no build is given, default is 366